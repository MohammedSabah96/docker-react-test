sudo: required
services:
  - docker

before_install:
  - docker build -t mohammeds96/docker-react-test -f  Dockerfile.dev  .

script:
  - docker run -e CI=true mohammeds96/docker-react-test npm run test